package main

import (
	"fmt"
	"html"
	"log"
	"os"
	"path/filepath"
	"reflect"
	"strings"
	"text/template"

    {{- range $key, $value := .}}
    pkg{{$key}} "{{$value}}"
    {{- end}})

type ConfigEnvField struct {
	Name         string
	DefaultValue string
	Type         string
	Description  string
}

func main() {
fmt.Println("Generating documentation for environment variables:")
content, err := os.ReadFile("templates/markdown/environment.md.tmpl")
if err != nil {
    log.Fatal(err)
}
replacer := strings.NewReplacer(
		"github.com/opencloud-eu/opencloud/services/", "",
		"/pkg/config/defaults", "",
	)
var fields []ConfigEnvField
var targetFile *os.File
tpl := template.Must(template.New("").Parse(string(content)))

m := map[string]interface{}{
{{- range $key, $value := .}}
	"{{$value}}": *pkg{{$key}}.FullDefaultConfig(),
{{- end }}
}

    targetFolder := "../../docs/services/_includes/"
    err = os.MkdirAll(targetFolder, 0755)
    if err != nil {
        log.Fatalf("Failed to create target folder: %s", err)
    }
    for pkg, conf := range m {
	fields = GetAnnotatedEnvVariables(conf)
	if len(fields) > 0 {
	fmt.Printf("... %s\n", pkg)
		targetFile, err = os.Create(filepath.Join(targetFolder, replacer.Replace(pkg) + "_configvars.md"))
		if err != nil {
			log.Fatalf("Failed to create target file: %s", err)
		}
		defer targetFile.Close()
		if err := tpl.Execute(targetFile, fields); err != nil {
			log.Fatalf("Failed to execute template: %s", err)
		}
	}
    }
	fmt.Println("done")
}

func GetAnnotatedEnvVariables(s interface{}) []ConfigEnvField {
	t := reflect.TypeOf(s)
	v := reflect.ValueOf(s)

	var fields []ConfigEnvField
	for i := 0; i < t.NumField(); i++ {
		field := t.Field(i)
		value := v.Field(i)

		switch value.Kind() {
		default:
			desc := field.Tag.Get("desc")
			env, ok := field.Tag.Lookup("env")
			if !ok {
				continue
			}
			v := fmt.Sprintf("%#v", fmt.Sprint(value.Interface()))
			typeName := value.Type().Name()
			if typeName == "" {
				typeName = value.Type().String()
			}

			fields = append(fields, ConfigEnvField{Name: strings.ReplaceAll(env, ";", "<br/>"), DefaultValue: html.EscapeString(strings.Replace(v, "|", "\\|", -1)), Description: desc, Type: typeName})
		case reflect.Ptr:
			// PolicySelectors in the Proxy are being skipped atm
			// they are not configurable via env vars, if that changes
			// they are probably added to the Sanitize() function
			// and this should not be an issue then
			if !value.IsZero() && value.Elem().CanInterface() {
				fields = append(fields, GetAnnotatedEnvVariables(value.Elem().Interface())...)
			}
		case reflect.Struct:
			fields = append(fields, GetAnnotatedEnvVariables(value.Interface())...)
		}
	}
	return fields
}
